name: Samurai Campaign Callbacks v1
kind: cloud
inputs:
  fields:
  - id: callbacks
    label: Scoped campaign callbacks
    type: string
    secret: true
  required:
  - callbacks
injectors:
  file:
    template: '{{ callbacks }}'
  env:
    SAMURAI_CAMPAIGN_CALLBACKS_FILE: '{{ tower.filename }}'
